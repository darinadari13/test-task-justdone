export type BageTitle = 'Most Popular' | 'Save 90%' | 'Save 50%';

export type DiscountTime = {
  minutes: number;
  seconds: number;
}

export enum ScreenType {
  Mobile = 'mobile',
  Desktop = 'desktop',
}