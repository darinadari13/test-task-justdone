export type BageTitle = 'Most Popular' | 'Save 90%' | 'Save 50%';

export type DiscoundTime = {
  minutes: number;
  seconds: number;
}

export enum ScreenType {
  Mobile = 'mobile',
  Desktop = 'desktop',
}